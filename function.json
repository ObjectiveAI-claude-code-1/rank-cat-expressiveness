{
  "type": "vector.function",
  "input_maps": [
    {
      "$starlark": "input"
    }
  ],
  "tasks": [
    {
      "type": "vector.function",
      "owner": "ObjectiveAI-claude-code-1",
      "repository": "rank-cat-expression-legibility",
      "commit": "d8609645eb33f1240b1d4f6085dd61a124d99773",
      "skip": null,
      "input": {
        "$jmespath": "input"
      },
      "output": {
        "$jmespath": "output"
      }
    },
    {
      "type": "vector.function",
      "owner": "ObjectiveAI-claude-code-1",
      "repository": "cat-body-action-signal",
      "commit": "dddb3c5f57cfeb922c1cd6958e965b6cb699ac0a",
      "skip": null,
      "input": {
        "$jmespath": "input"
      },
      "output": {
        "$jmespath": "output"
      }
    },
    {
      "type": "placeholder.scalar.function",
      "input_schema": {
        "type": "image",
        "description": "A single cat picture to evaluate for how particular and singular the captured moment is."
      },
      "skip": null,
      "map": 0,
      "input": {
        "$jmespath": "map"
      },
      "output": {
        "$starlark": "[x / sum(output) for x in output] if sum(output) > 0 else [1.0 / len(output) for x in output]"
      }
    }
  ],
  "input_split": {
    "$starlark": "[[item] for item in input]"
  },
  "input_merge": {
    "$starlark": "[item for sublist in input for item in sublist]"
  },
  "description": "Ranks cat pictures by expressive character. Evaluates facial legibility, bodily intention, and the particularity of the captured moment to surface images where the cat's personality speaks through the frame. Specific, characterful poses rank high; generic, interchangeable snapshots rank low.",
  "input_schema": {
    "type": "array",
    "description": "A collection of cat pictures to rank by the expressive character of the cat in each image.",
    "minItems": 2,
    "items": {
      "type": "image",
      "description": "A cat picture to evaluate for expressive character."
    }
  },
  "output_length": {
    "$starlark": "len(input)"
  }
}